//Q2. Write a function called "multiplyNumbers" that take the two number as arguments and return their product.Use function expression to define the function
//and call the function before it is declared to demonstarate hosting.

const multiplyNumbers=function(x,y){
     return x*y;
}
console.log(multiplyNumbers(40,3));       //120;

