//Q3. Write a function that takes two number as argumnets and return their sum.Declare a variable inside the function using the var keyword and log its value to the console 
//before its assigned value to demostrate variable hosting.

function addNumber(x,y){
     console.log(sum);        //Undefined
     var sum;
     sum=x+y;            
     return sum;
}
console.log(addNumber(500,330));         //838;


